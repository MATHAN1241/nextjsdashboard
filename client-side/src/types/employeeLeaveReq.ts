export type EmployeeLeaveRequest = {
    firstName: string; // DisplEmployeeLeaveRequestay picture (image path)
    employeeId: string;
    fromDate: string;
    toDate: string;
    reason: string;
    status:string;
    _id:string
  };